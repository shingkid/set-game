import { useGameStore } from '../../game/store'

// ---------------------------------------------------------------------------
// TODO (Issue #5): Heads-up display — score, timer, and controls.
//
// Sections to include:
//
//   Score     — "Sets found: {setsFound}" from the store
//   Deck      — "{deck.length} cards remaining"
//   New Game  — button that calls startGame()
//   Hint      — button that highlights one valid Set on the board
//               (implement after findAllSets works in Issue #1)
//   Timer     — elapsed time; start simple (count up from 0)
//               Add to the store in Issue #5: startTime, a tick action,
//               and a useEffect in this component for the interval
//
// Keep it simple for now — you can style it properly in Issue #6.
// ---------------------------------------------------------------------------
export function HUD() {
  void useGameStore
  // TODO
  return null
}
